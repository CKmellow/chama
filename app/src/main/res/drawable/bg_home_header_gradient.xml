<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The blue gradient background -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="@color/primary_dark"
                android:endColor="@color/primary"
                android:type="linear" />
        </shape>
    </item>
    <!-- The white curved shape overlay -->
    <item android:top="150dp"> <!-- Adjust this value to control how much of the gradient is visible -->
        <shape android:shape="rectangle">
            <solid android:color="@color/white_background" />
            <corners android:topLeftRadius="24dp" android:topRightRadius="24dp" />
        </shape>
    </item>
</layer-list>
